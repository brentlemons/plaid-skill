# --- Secrets / credentials (Plaid keys, tokens) ---
.env
.env.*
!.env.example
secrets.json
credentials.json
*.pem
.plaid/

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so

# Packaging / build
build/
dist/
sdist/
wheels/
*.egg-info/
*.egg
.eggs/
MANIFEST

# Virtual environments
.venv/
venv/
env/
ENV/

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.pyre/
.pytype/
.dmypy.json
dmypy.json
.tox/
.nox/
.hypothesis/
.ipynb_checkpoints/

# Coverage
.coverage
.coverage.*
coverage.xml
htmlcov/
*.cover
nosetests.xml

# Type stubs installed locally
.pyright/

# --- Editors / OS ---
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# --- Claude Code local state ---
# The AI-DLC framework shell that built this skill. Excluded for the same
# reason as aidlc/: the skill is the deliverable, the framework is working
# material. 3.2 MB across 257 files that would otherwise bury the actual
# repository contents for anyone cloning it.
.claude/

# --- AI-DLC workflow record ---
# Working material, not part of the published skill. The record holds the
# audit trail, stage artifacts, and interview transcripts for how this
# repository was built; the deliverable is the skill directory, the tests,
# and the README.
aidlc/

# --- Misc ---
*.log
tmp/
scratch/
